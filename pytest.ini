[pytest]
testpaths = tests
pythonpath = .

# simulator is a deliberately archived product surface; its historical tests
# remain under archive/ and are not part of the active market-data suite.
norecursedirs = archive .git .cache .pytest_cache
